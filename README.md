Команды для генерации gRPC:
`make go-grpc`
`make py-grpc`
`make grpc`
Проблемы с относительными путями к config.yaml, решить через env переменную с путём к корню

На телефоне у кнопок управления сайта только иконки, текст убирается